Event Management Frontend (React + Vite)
========================================
1) Install & run
   npm install
   npm run dev
2) Ensure backend API is running at http://localhost:3000
3) The API base is set in .env:
   VITE_API_BASE=http://localhost:3000
